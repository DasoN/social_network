import 'package:flutter/material.dart';
import 'package:sn/screens/home.dart';

class profileLog extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // final InitialUser user = Provider.of<InitialUser>(context);
    // bool _isLogginIn = user != null;
    // print(user.id);
    return home();
  }
}
